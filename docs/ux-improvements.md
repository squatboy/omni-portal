# UX 개선 사항

## 스크롤바 레이아웃 흔들림(Layout Shift) 방지

### 문제 상황
페이지 콘텐츠의 길이에 따라 스크롤바가 생기거나 없어질 때, 화면 내 컴포넌트들이 가로로 미세하게 움직이는 레이아웃 흔들림 현상 발생.

### 해결 방법
글로벌 스타일(`frontend/app/globals.css`)의 `html` 선택자에 `scrollbar-gutter: stable` 속성을 추가함.
이를 통해 스크롤바가 존재하지 않는 상태에서도 스크롤바 영역을 미리 확보하여, 페이지 길이에 관계없이 컴포넌트들의 레이아웃 위치가 항상 일정하게 유지되도록 개선함.

## 다크/라이트 모드 전환 토글 버튼 및 핫키 기능

### 구현 내용
- 사용자가 마우스 클릭을 통하거나 단축키(핫키)를 사용해 편리하게 다크 모드와 라이트 모드를 토글할 수 있는 기능 제공.

### 상세 내용
1. **헤더 내 토글 버튼 제공**
   - 대시보드 헤더 우측의 `Logout` 버튼과 `Refresh` 버튼 사이에 테마 토글 버튼 추가.
   - 현재 적용된 테마(Dark/Light)에 맞게 각각 Sun, Moon 아이콘이 노출되도록 디자인함.
2. **단축키(핫키) 매핑**
   - 페이지 내 어디서든 단축키 `d` 또는 `D` 키를 누르면 테마가 토글됨.
   - 단, 사용자가 검색창이나 입력란(`input`, `textarea` 등)에서 텍스트를 입력하는 중에는 단축키 오작동을 방지하기 위해 필터링 처리됨.
